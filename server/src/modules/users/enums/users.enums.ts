export enum USER_STATES {
  ACTIVE = 'active',
  PENDING = 'pending',
  INACTIVE = 'inactive',
  EMPTY = '',
}

export enum USER_ROLES {
  ADMIN = 'admin',
  EMPLOYEE = 'employee',
  CLIENT = 'client',
  EMPTY = '',
}
